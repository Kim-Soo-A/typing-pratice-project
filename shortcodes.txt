#include <stdio.h>
#include <string.h>
void main() {
printf("Hello, World!");
int a=0;
for(int i=0; i<10; i++) {
while(i>0) {
scanf("%d", &num);
return 0;
break;
continue;
float avg;
scanf("%f", &avg);
char name[100];
int a, b, c, max;
else if (b==c&&a!=b)
printf("%d", 1000 + b * 100);
if (result == 1)
switch (num) {
case 1:
